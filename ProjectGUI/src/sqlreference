create table adminlogin(adminid varchar(100),adminname varchar(100))

insert into adminlogin values('admin','admin')

select * from adminlogin;

create table addproduct(pid integer,pname varchar(100),pprice varchar(100))

select * from addproduct